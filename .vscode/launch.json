{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Angular Debugg",
      "request": "launch",
      "command": "npm run start",
      "cwd": "${workspaceFolder}"
    }
  ]
}